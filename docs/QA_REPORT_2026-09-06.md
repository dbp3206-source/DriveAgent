# Báo cáo QA live — 2026-09-06

## Phạm vi

Runtime local tại `http://localhost:8000`, dùng OAuth/Drive/Gemini thật của người dùng.
Secret, OAuth token và request header không được đưa vào log hoặc báo cáo.

## Các lỗi đã tìm và sửa

- OAuth credential legacy có timezone không tương thích SDK mới: chuẩn hóa UTC và dùng
  serializer chính thức của Google.
- Agent lặp `rag_search` đến hard limit: thêm guard theo chữ ký tool call và node tổng hợp.
- Notebook `.ipynb` đưa output/base64 vào RAG: chỉ trích Markdown/code cell source.
- Citation 500 ký tự cắt mất phần giải thích: tăng excerpt có giới hạn lên 1.800 ký tự.
- Drive chỉ hiển thị trang đầu: thêm phân trang và nút tải thêm.
- Chat có thể hiện response cũ khi đổi session: hủy cập nhật stale và khóa thao tác khi busy.
- Settings tràn ngang trên mobile: sửa intrinsic grid sizing và wrapping.
- Audit bỏ sót lần gọi bị chặn trước execution: tạo audit trước schema/RBAC/scope/rate limit.
- Dependency có advisory mới: nâng `cryptography` và `pytest` lên bản đã vá.

## Kết quả tự động

- Backend: 29 passed.
- Ruff: passed.
- Frontend ESLint và production build: passed.
- `pip check`: passed.
- `pip-audit`: không có lỗ hổng đã biết trong dependency có thể audit.
- `npm audit --omit=dev`: 0 vulnerability.
- `git diff --check`: passed.

## Kết quả live

- Primary `gemini-3.8-flash` và fallback `gemini-3.5-flash-lite`: gọi thật thành công.
- `gemini-embedding-2`, vector 768D: index và semantic retrieval thật thành công.
- Drive: list, pagination, search, read thành công.
- RAG: index, idempotent re-index, retrieval, answer và Drive citation thành công.
- Memory: save, semantic search, Agent retrieval và persistence qua restart thành công.
- Audit/RBAC/Settings: dữ liệu thật hiển thị đúng; health toàn bộ ready.
- Browser: 390 px, 768 px và viewport desktop không overflow; dark/light hoạt động;
  focus hiển thị; không có console warning/error.
- Cách ly user: integration tests xác nhận chat, RAG, Memory và vector query đều lọc
  server-side theo `user_id`.

## Giới hạn nghiệm thu thủ công

Không tự động đăng nhập tài khoản Google thứ hai vì OAuth cần chính người dùng chấp thuận.
Đây không phải khoảng trống logic: các đường đọc chat/RAG/Memory đã có integration test
cách ly; profile Google thứ hai chỉ là bước xác nhận vận hành bổ sung nếu nhóm sử dụng thật.
